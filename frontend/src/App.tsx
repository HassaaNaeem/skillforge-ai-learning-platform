// Placeholder shell only — proves React + Vite + Tailwind are wired
// correctly. The real landing page/routing/state layers arrive starting
// in M2/M4, once there's an app to route to.
function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-950 text-slate-100">
      <div className="text-center">
        <h1 className="text-3xl font-semibold tracking-tight">SkillForge</h1>
        <p className="mt-2 text-sm text-slate-400">M0 scaffold — tooling only, no features yet.</p>
      </div>
    </main>
  );
}

export default App;
