# modules

One folder per resource (auth, topics, practice, interview, learningPlan, savedQuestions, upload, ai), each with `routes.ts`, `controller.ts`, `service.ts`, `schema.ts`. First modules (auth, topics) arrive in M2/M3.
