// Placeholder export so the package builds. Shared Zod schemas and
// inferred types (used by both frontend and backend) will be added
// starting in M1/M2, once there is an actual API contract to share.
export const SHARED_PACKAGE_NAME = '@skillforge/shared';
